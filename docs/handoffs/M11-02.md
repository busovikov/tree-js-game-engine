# Handoff: M11 Bounce Run vertical slice — final verification

## Objective

Finalize the engine-only Bounce Run vertical slice by proving the committed implementation
against every remaining repository gate in an isolated clean worktree, recording the existing
real-Chrome evidence without overstating it, and leaving an exact, bounded first RED for M12.

## Scope and acceptance criteria

- [x] A/D and arrows steer laterally; R restarts; Escape pauses.
- [x] A CCD Rapier sphere produces one analytic bounce per valid downward landing.
- [x] Bounce/controller/camera behavior is frame-rate independent and edge contacts are
      rejected.
- [x] Start, active, paused, game-over, resume, and restart UI transitions execute through a
      compiled graph.
- [x] The app uses public engine/service APIs and has no editor or React dependency.
- [x] The authored platform sequence is instantiated through the public prefab-backed
      `EntityPool`.
- [x] User Chrome can start, play, pause, resume, fail, and restart without application console
      errors.
- [x] Initial performance and bundle baselines plus provisional budgets are recorded.
- [x] `pnpm test`, `pnpm build`, `pnpm depcruise`, and `./scripts/check.sh` pass from a detached
      clean worktree at the committed M11 source state.
- [x] The primary worktree is left with only the two protected user files uncommitted, and the
      user's Vite server remains running.

## Required context

- `docs/autonomous-engine-game-agent.md`
- `docs/engine-game-development-plan.md` M11 and M12 only
- `docs/stage-handoff.md`
- `docs/handoffs/M11-01.md`
- `apps/bounce-run/src/`

## Decisions and invariants

- M11 is complete. No full gate exposed an M11 source regression, so no code or test change was
  justified during finalization.
- Full gates ran only in a detached temporary worktree at committed `479d764`. The ignored
  `apps/playground/public/assets/manifest.json` was generated there with the established scene
  generator; its six tracked Isaac scene rewrites were restored both before and after the gates.
- Clean-worktree `file:` dependencies required three successive
  `pnpm install --force --frozen-lockfile` repacks as newly built dependency layers became
  available. The intermediate missing-module build failures were packaging-order prerequisites,
  not M11 regressions; the final standalone build and aggregate check both passed.
- Existing user-Chrome evidence is retained rather than repeated because no runtime code changed
  after that QA. It proves the named flow and visible sustained bouncing, but it did not directly
  measure lateral displacement.
- A/D, arrow bindings, and deterministic lateral response are focused-test evidence only until
  M12 adds the public observation seam and replay-backed browser automation.
- M12 owns route generation, reachability, infinite pooling, replay, observations, reports, and
  automated Chrome E2E. No M12 implementation was started.

## Completed

- Verified the complete M11 source state through all remaining repository gates in isolation.
- Confirmed the production Bounce Run bundle remains within its M11 provisional ceiling.
- Marked M11 `Complete` in the milestone table and recorded exact gate evidence and the direct
  lateral-displacement limitation.
- Preserved the protected playground project/HUD and the user's Vite process unchanged.
- Removed the detached temporary worktree after checking that its tracked tree was clean.

## Files changed

- `docs/engine-game-development-plan.md`: mark M11 complete and record final gate evidence and
  the exact browser-evidence limitation.
- `docs/handoffs/M11-02.md`: final M11 operational record and M12 entry action.

## Local commits

- `e690a81` — `Track one valid landing per fixed tick`
- `c7ebd58` — `Make Bounce Run bounce and lateral control deterministic`
- `dc15df2` — `Add frame-rate-independent Bounce Run camera`
- `291976e` — `Execute Bounce Run session and HUD transitions in graphs`
- `4e9dae7` — `Instantiate pooled prefabs through the public component registry`
- `28069af` — `Package the Bounce Run scene, prefab, and HUD assets`
- `13b75fe` — `Hide flex UI containers from browser layout`
- `8bfbc0f` — `Run Bounce Run through fixed physics and pooled platforms`
- `479d764` — `Record the M11 verification rollover`
- `cef732a` — `Mark M11 complete after repository gates`

## Verification evidence

- `node scripts/generate-isaac-sketch-scenes.mjs` in the detached worktree — generated the
  ignored 159-file playground manifest; the six automatically rewritten tracked Isaac scenes
  were restored explicitly before the gates and again after them.
- `pnpm install --frozen-lockfile` in the detached worktree — passed for all 22 workspace
  projects.
- `pnpm test` — passed: 189 test files, 739 tests passed, 8 skipped; includes the Bounce Run
  fixed-step Rapier integration. Rapier printed its known upstream initialization deprecation
  warning.
- First `pnpm build` attempt — failed because the freshly isolated `file:` copy of
  `@haku/physics` predated the new `@haku/core` build output; not a source regression.
- `pnpm install --force --frozen-lockfile` then second `pnpm build` attempt — progressed through
  physics and failed at the next `file:` layer because `@haku/engine` copies predated freshly
  built serializer/Rapier output; not a source regression.
- `pnpm install --force --frozen-lockfile` then third `pnpm build` attempt — progressed through
  engine and both game apps, then failed at the final `file:` layer because the editor app copy
  predated freshly built `@haku/editor`; not a source regression.
- `pnpm install --force --frozen-lockfile` then final `pnpm build` — passed all 21 runnable
  workspace projects. Bounce Run built 187 modules with `3,168.09 kB` minified / `1,075.23 kB`
  gzip JavaScript; the expected provisional chunk-size warning remains.
- `pnpm depcruise` — passed: no new dependency violations across 572 modules and 1,109
  dependencies; 5 known baseline violations ignored.
- `./scripts/check.sh` — passed its install, all-project build, 189-file/739-test suite, and
  playground bundle audit; final output was `OK: all checks passed`.
- Browser check from the M11 implementation session — user Chrome at
  `http://127.0.0.1:5191/` showed start, active play with sustained collision-driven bouncing,
  pause, resume, end-of-track game over, and restart; rolling HUD evidence was `8.3 ms`,
  `120 FPS`, 16 entities, and pool `6/6`, with no application console errors. Direct lateral
  displacement was not instrumented or measured. The temporary Bounce Run server was stopped.
- Final detached-worktree check — tracked status clean after the second explicit six-scene
  restore; the ignored generated manifest existed only there; the worktree was removed.

## Current repository state

- Branch: `engine-improvement-based-on-game-creation`
- HEAD before this handoff commit: `cef732a`
- Worktree contains this handoff plus exactly the user's two protected live files:
  - modified `apps/playground/haku.project.json`, 3,238 bytes, mtime
    `2026-07-30 00:30:31 +0700`;
  - untracked `apps/playground/public/assets/ui/hud.ui.json`, 671 bytes, same mtime.
- Neither protected file was read, staged, restored, deleted, reformatted, overwritten, or
  copied during finalization.
- User Vite PID 69639 remains listening on port 5174 and was not stopped.
- No gate worktree or M11-owned server remains.

## Remaining work

- M11 has no remaining implementation or verification work.
- M12 must implement the deterministic route generator, analytic reachability plus boundary
  simulation, guaranteed fallback, infinite pooled route, fixed-tick replay/hashes, public
  observation/assertion APIs, reports, mass-seed/Rapier comparison suites, and Chrome E2E.
- M13/M14 still own long-run device-tier and memory-growth measurements, production chunk
  splitting, and the final export audit.

## Risks and open defects

- The M11 performance baseline covers one Chrome/device session and a finite six-platform route;
  it is not long-run performance or memory-growth evidence.
- The production game JavaScript remains a single large chunk. It is inside the M11 provisional
  `3.5 MB` minified / `1.2 MB` gzip ceiling; splitting remains M14 scope.
- Rapier emits its known upstream compatibility initialization deprecation warning. No M11
  application warning or error is known.
- Direct Chrome lateral displacement is not measured. Focused input/controller tests prove the
  binding and deterministic response; measured browser evidence waits for M12's public
  observation API.
- Clean isolated builds currently need staged `file:` dependency repacks as dependency outputs
  become available. This is a repository packaging-order limitation, not an M11 runtime defect.

## Exact next action

Start a fresh M12 stage agent. Create `apps/bounce-run/src/route-generator.test.ts` importing the
intentionally missing `generateBounceRunRoute` from `./route-generator.js`. In one focused test,
call it twice with the same immutable input `{ seed: 0x5eed, platformCount: 64 }` and assert deep
equality of the complete returned platform sequence and decision log. Run
`pnpm exec vitest run apps/bounce-run/src/route-generator.test.ts` and capture the expected
module-not-found RED before defining the generator contract or implementation.

## Context exclusions

- Do not read or modify `apps/playground/haku.project.json` or
  `apps/playground/public/assets/ui/hud.ui.json` without explicit new user authority.
- Do not reopen M10 service internals, editor/React UI, provider SDKs, deployment, M13 polish, or
  M14 export work for the first M12 generator RED.
- Do not repeat M11 browser QA unless M12 changes the runtime path or a regression requires it;
  when browser work is required, use only the user's Chrome.
