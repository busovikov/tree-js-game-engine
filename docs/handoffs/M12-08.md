# Handoff: M12 final acceptance audit and completion

## Objective

Complete the final M12 audit strictly against the milestone's written acceptance criteria, prove
the committed Chrome workflow evidence and production QA boundary, run every repository gate from
a detached clean worktree, and hand M13 one exact first RED without starting M13 implementation.

## Scope and acceptance criteria

- [x] Same seed/config produces identical route decisions — the committed generator evidence and
      `apps/bounce-run/src/route-generator.test.ts` cover deterministic decisions.
- [x] Every generated sequence has a reachable path and deterministic fallback — generator,
      reachability, and infinite-route tests cover the bounded fallback and continuous main route.
- [x] Safe start and decreasing non-zero difficulty margins are covered by the committed generator
      tests and M12 plan evidence.
- [x] Mass generation covers 256 seeds × 128 platforms without intersections, invalid gaps,
      missing routes, or unbounded attempts; the full suite passed the generator and infinite-route
      tests.
- [x] Replay either matches every state hash or reports the exact divergence —
      `apps/bounce-run/src/replay.test.ts` passed all 3 tests in the full suite.
- [x] QA uses public actions and immutable observations without a game-state mutation handle — the
      harness, observation, structured-observation, and report tests all passed.
- [x] Browser E2E covers start, input, pause, game over, restart, resize, and console errors — the
      bounded committed user-Chrome report records repeated real `KeyD` presses with measured DEV
      displacement plus both DEV and production lifecycle/resize/console flows.
- [x] QA is absent from production export — the real-production-build regression passed, all 4 QA
      source sentinels were present, and all 8 authoritative QA production exclusions were absent
      from the emitted JavaScript.
- [x] Full detached-worktree lint, typecheck, test, build, dependency, umbrella, and bundle gates
      passed; M12 is marked Complete.

## Required context

- `docs/autonomous-engine-game-agent.md`
- `docs/engine-game-development-plan.md` M12 and the opening M13 section only
- `docs/node-graph-architecture.md` QA, scheduler, and production-export constraints
- `docs/stage-handoff.md`
- `docs/handoffs/M12-08.md`
- `apps/bounce-run/src/route-generator.test.ts`
- `apps/bounce-run/src/route-generator.ts`

## Decisions and invariants

- M12 completion is governed by the written milestone acceptance rows, not by stronger extension
  wishes introduced in the prior handoff.
- Repeated real Chrome-extension `locator.press('KeyD')` events with measured DEV displacement are
  browser input evidence for the written row. The separate public hold-right action remains only
  supporting sustained-control evidence.
- The installed Chrome extension exposes neither separate physical key down/up nor production
  `requestfailed` events. Those remain explicit non-blocking external-tooling risks; this audit does
  not claim either kind of evidence.
- Browser QA was not repeated because the committed bounded report already covers the unchanged
  runtime. The report validator and production regression were rerun instead.
- Initial clean-worktree failures caused by successive stale `file:` package copies were packaging
  order, not source regressions. Three `pnpm install --force --frozen-lockfile` repacks advanced the
  build through physics, engine, and editor application layers before the full build passed.
- No M13 runtime, test, gameplay, or presentation code was added in this stage.

## Completed

- Built the written M12 acceptance matrix from the plan, committed browser report, and focused/full
  test evidence; all eight rows are green.
- Validated the immutable bounded Chrome report and the real production QA exclusion regression.
- Audited the built Bounce Run bundle directly: 4/4 authoritative source sentinels present and 8/8
  production exclusions absent from its single JavaScript file.
- Ran every required full gate from detached committed HEAD `ffcc687` after generating the ignored
  159-file playground asset manifest and explicitly restoring all six generator-touched scenes.
- Marked M12 Complete in the milestone map and acceptance list with exact gate evidence and honest
  extension limitations.
- Re-restored the six generated scenes, confirmed tracked-clean temp status, and removed the temp
  worktree.

## Files changed

- `docs/engine-game-development-plan.md`: mark M12 Complete and record final acceptance, gate,
  production-exclusion, and non-blocking tooling-risk evidence.
- `docs/handoffs/M12-08.md`: final M12 evidence and exact first M13 RED.

## Local commits

- `444029e` — `Mark M12 complete after final gates`
- Handoff documentation commit is created separately after this file is staged and verified.

## Verification evidence

- `pnpm exec vitest run apps/bounce-run/src/browser-workflow-report.test.ts apps/bounce-run/src/production-qa-boundary.test.ts`
  — passed 2 files / 4 tests; the production test ran the real build and excluded every
  authoritative DEV QA sentinel.
- Direct production audit — the one emitted Bounce Run JavaScript file retained 4/4 source
  sentinels at their sources and contained 0/8 production QA exclusions.
- Evidence artifact — `docs/evidence/m12-browser-workflow-report.v1.json` validated through the
  focused test; SHA-256
  `023bef00b8691af0932e9f70d8981c2ec39c5af0f63b1c280ffeb23e1034bb0f`.
- `pnpm lint` — passed with no ESLint findings.
- `pnpm typecheck` — passed all 21 runnable workspace projects.
- `pnpm test` — passed 201 test files and 801 tests; 1 file and 8 tests skipped.
- `pnpm build` — passed all 21 runnable workspace projects after three documented clean-worktree
  `file:` repacks; Bounce Run built 193 modules and emitted `3,175.48 kB` minified /
  `1,077.70 kB` gzip JavaScript with only the established chunk-size advisory.
- `pnpm depcruise` — passed 597 modules and 1,157 dependencies with no new violations; 5 known
  violations were ignored by the configured baseline.
- `./scripts/check.sh` — passed its install, all-project build, repeated 201-file/801-test suite,
  and playground bundle audit; final output was `OK: all checks passed`.
- Temp prerequisite and cleanup — generated ignored `apps/playground/public/assets/manifest.json`,
  explicitly restored six generator-touched tracked scene files before gates and again after them,
  confirmed tracked status clean, and removed `/tmp/haku-m12-gates.SX2MgD`.
- Browser evidence — the unchanged committed user-Chrome report covers start; six real repeated
  `KeyD` presses moving DEV `x 0.000` at tick 24 to `x 0.220` at tick 57; pause with stable ticks;
  game over; immediate restart; 1280×720 and 1600×900 resize; and no unexpected console diagnostic
  beyond the exact whitelisted Rapier initialization warning. No separate keydown/up or
  `requestfailed` evidence is claimed.

## Current repository state

- Branch: `engine-improvement-based-on-game-creation`
- HEAD before the separate handoff commit: `444029e`
- Worktree before the handoff commit contains this new handoff plus exactly the protected user
  changes:
  - modified `apps/playground/haku.project.json`;
  - untracked `apps/playground/public/assets/ui/hud.ui.json` (Git may report its containing
    directory).
- Neither protected file was read, modified, staged, restored, deleted, reformatted, overwritten,
  or copied.
- User Vite PID 69639 remains the listener on port 5174 and was not stopped or replaced.
- No gate worktree or M12-owned server remains.

## Remaining work

- M12 has no remaining acceptance or verification work.
- Start M13 only in a fresh semantic-stage agent with the exact RED below; do not broaden that first
  slice into presentation, saves, audio, or stabilization.

## Risks and open defects

- The installed Chrome extension cannot produce separate physical multi-tick key down/up evidence.
  This is a tooling limitation, not a written M12 acceptance failure.
- The installed Chrome extension cannot expose the production `requestfailed` stream. The real
  production build, QA exclusion regression, visible asset-complete workflow, and clean console are
  green, but this handoff does not claim an authoritative failed-request observation.
- Bounce Run still emits the established Vite large-chunk advisory within the provisional M11
  ceiling; final bundle optimization/audit remains M14 work.

## Exact next action

Dispatch a fresh M13 agent. Its first and only initial RED is a focused pure-generator test using
one fixed seed and difficulty schedule that requires generated normal, wide, narrow, and bounce
platform variants plus one bonus whose reachability passes the analytic envelope. Run
`pnpm exec vitest run apps/bounce-run/src/route-generator.test.ts` and record that this new test
fails before adding any M13 gameplay or presentation code.

## Context exclusions

- Do not read or modify `apps/playground/haku.project.json` or
  `apps/playground/public/assets/ui/hud.ui.json`.
- Do not reopen editor, production export, M12 browser QA, or M14 during the first M13 RED.
- Do not add M13 code before the fixed-seed variant-and-bonus generator test is observed failing.
- Do not stop or replace the user's Vite process on port 5174.
