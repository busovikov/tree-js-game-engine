# Handoff: M13 bounded gameplay effects and ball trail

## Objective

Complete the next separate M13 semantic slice: compose bounded landing, bonus-pickup, and fail
effects plus a readable ball trail through reusable engine/runtime lifecycle boundaries. Do not
reopen the completed production audio lifecycle.

## Scope and acceptance criteria

- [ ] Add the mandatory effects integration RED before production effects or trail changes.
- [ ] One valid landing emits exactly one bounded landing burst; repeated/side/backwards contact
      cannot duplicate it.
- [ ] One collected pooled bonus emits exactly one pickup effect and stale/reused trigger events do
      not duplicate it.
- [ ] One fail transition emits exactly one fail effect and restart does not retain it.
- [ ] The ball trail is visually bounded and cleans every runtime object on restart and dispose.
- [ ] Pool release/reacquire/reset clears owned effects with no stale handles/subscriptions.
- [ ] Repeated bounded events and restarts do not grow active render/effect object counts.
- [ ] Focused effects/pool/graph tests, all Bounce Run tests, typecheck/build, and user-Chrome smoke
      pass with only the established Rapier warning.

## Required context

- `docs/autonomous-engine-game-agent.md`
- `docs/engine-game-development-plan.md` M13 only
- `docs/node-graph-architecture.md` render, graph event, activation, and pool lifecycle sections
- `docs/stage-handoff.md`
- `docs/handoffs/M13-04.md`
- `apps/bounce-run/src/game-systems.ts`
- `apps/bounce-run/src/session-runtime.ts`
- `apps/bounce-run/src/session-graph.ts`
- `apps/bounce-run/src/infinite-route.ts`
- Narrow public render/presentation/pool lifecycle entrypoints found through `rg`

## Decisions and invariants

- Audio is complete and out of scope. Do not change audio clips, buses, unlock ordering,
  pause/resume, music ownership, audio graph nodes, or audio HUD controls.
- LandingTracker remains the once-per-tick landing authority. Effects consume the accepted landing
  boundary and must not introduce another contact interpretation.
- Bonus collection remains atomic against the active pooled lease; the effect follows the accepted
  collection and never makes stale overlap events authoritative.
- Effects/trail runtime resources must use a reusable engine lifecycle/service boundary. Do not add
  an app-local unbounded object array or QA mutation backdoor.
- The next later stage owns long-run/resize/30 FPS stabilization; this effects slice proves only
  bounded local lifecycle and no growth under focused repetition.

## Completed

- M13 production audio lifecycle is complete: gesture unlock, typed retryable failures, authored
  graph SFX, singleton music, graph-owned Master/Music/SFX/UI settings, pause/resume/restart, and
  pooled-owner cleanup.
- Minimal local procedural WAV assets keep production offline and static-export compatible.
- User Chrome verified Start, Escape pause, Resume, mute settings, restart, zero QA diagnostics, and
  no autoplay error through observable UI/state; subjective audibility was not claimed.

## Files changed

- `packages/audio/src/index.ts`: typed/idempotent unlock and pause lifecycle plus read-only state.
- `packages/pool/src/index.ts`: removable late lifecycle participant registration.
- `apps/bounce-run/src/audio-composition.ts`: production audio ownership and UI activation chain.
- `apps/bounce-run/src/session-graph.ts`: authored SFX and bus setting graph nodes.
- `apps/bounce-run/src/session-runtime.ts`: public audio graph registrar and settings entries.
- `apps/bounce-run/public/assets/ui/hud.ui.json`: accessible always-available audio controls.
- Adjacent focused tests, package dependencies, API/edge docs, and M13 evidence.

## Local commits

- `070547d` — `Specify Bounce Run production audio lifecycle`
- `291a093` — `Make audio lifecycle transitions typed and pool-aware`
- `e75b47a` — `Compose Bounce Run production audio lifecycle`
- `3c2134d` — `Refresh Bounce Run audio composition assertions`
- `fc9eb33` — `Keep Bounce Run audio controls available while paused`
- `facf393` — `Record M13 production audio evidence`

## Verification evidence

- Mandatory RED: `pnpm exec vitest run apps/bounce-run/src/audio-composition.integration.test.ts`
  — failed before production changes because `./audio-composition.js` did not exist.
- Focused GREEN: audio composition, session/game integration, audio/audio-web, and pool lifecycle —
  9 files / 36 tests passed.
- Affected broad gate: `pnpm exec vitest run apps/bounce-run/src packages/audio/src
  packages/audio-web/src packages/pool/src packages/graph/src packages/graph-runtime/src
  packages/ui/src` — 58 files / 224 tests passed; only the established Rapier warning appeared.
- `pnpm --filter @haku/audio build`, `pnpm --filter @haku/audio-web build`, and
  `pnpm --filter @haku/pool build` — passed.
- `pnpm --filter @haku/bounce-run typecheck` — passed.
- `pnpm --filter @haku/bounce-run build` — passed, 200 modules; 3,217.55 kB minified /
  1,089.34 kB gzip JS; established chunk-size advisory only.
- User Chrome on agent-owned `http://127.0.0.1:5193/` — Start reached active without autoplay
  error; Escape/Resume, Master/Music mute, and restart with retained settings worked; QA diagnostics
  stayed zero; console contained only the exact Rapier warning. Agent server stopped.

## Current repository state

- Branch: `engine-improvement-based-on-game-creation`
- HEAD before this separate handoff commit: `facf393`
- Worktree contains only protected user changes plus this handoff before commit:
  modified `apps/playground/haku.project.json` and untracked `apps/playground/public/assets/ui/`.
- Neither protected path was read, modified, staged, restored, deleted, reformatted, overwritten, or
  copied. User Vite PID 69639 remains the listener on port 5174.

## Remaining work

- Implement the bounded effects/trail slice below.
- Then perform the separate long-run pool/object/memory/FPS, resize, and 30 FPS stabilization stage.
- M14 export and final quality audit remain out of scope until M13 is complete.

## Risks and open defects

- Chrome evidence proves browser state and controls, not subjective sound audibility.
- The local procedural music loop is intentionally minimal; advanced effect chains, streaming,
  occlusion, waveform editing, and advanced mixer UI remain deferred.
- Full M13 visual coherence, effects/trail, and long-run stabilization acceptance remain open.

## Exact next action

Before changing production render/effect code, add
`apps/bounce-run/src/effects-composition.integration.test.ts` using the real Bounce Run world,
public session/landing/bonus boundaries, real platform and bonus `EntityPool` leases, and a
controllable public presentation/effects backend or the smallest proven reusable lifecycle seam.
The first case must fail because no production effects composition exists, then require exactly one
landing burst for one accepted top/downward landing, exactly one bonus pickup effect for one claimed
lease, exactly one fail effect, and a bounded ball trail. Drive at least 100 repeated accepted events
and 20 restart/release/reacquire cycles; assert active effect/render counts return to the authored
baseline after restart, pool release/reset, and dispose, with zero owned handles/subscriptions and no
growth. Run only this file and capture its first failure before implementation. Do not import,
modify, spy on, or otherwise reopen any audio composition, audio graph, clip, backend, bus, unlock,
pause, or settings path.

## Context exclusions

- Do not read or modify `apps/playground/haku.project.json` or
  `apps/playground/public/assets/ui/hud.ui.json`.
- Do not read or modify Bounce Run audio composition, audio assets, audio graph nodes, or audio HUD
  controls unless an effects-only build break proves a direct consumer fix is unavoidable.
- Do not begin long-run stabilization, resize/30 FPS tuning, export, M14, storage redesign, or visual
  editor work inside the effects RED.
- Do not stop or replace the user's Vite process on port 5174.
