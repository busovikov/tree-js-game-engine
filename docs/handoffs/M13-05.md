# Handoff: M13 long-run runtime and presentation stabilization

## Objective

Complete the final separate M13 semantic stage: establish reproducible long-run evidence for bounded
pool/world/render/effect ownership, heap and frame behavior, then fix only proven stabilization,
30 FPS simulation, and Chrome resize defects. Do not begin M14 export or reopen completed audio and
bounded-effects composition.

## Scope and acceptance criteria

- [ ] Add the mandatory stabilization RED before production fixes.
- [ ] Run a production-like real Bounce Run world, Rapier scheduler, route, platform/bonus pools,
      graph session, and presentation effects long enough to expose sustained growth.
- [ ] Prove platform/bonus totals, active world entities, Three render objects, effect slots, trail
      points, subscriptions, queued graph work, and owned runtime handles return to their authored
      baseline through repeated fail/restart/release/reacquire cycles.
- [ ] Establish a warm heap baseline and retained-heap evidence without claiming unsupported browser
      precision; fix every reproducible sustained-growth defect.
- [ ] Prove 30 FPS render admission still produces fixed 60 Hz simulation, bounded catch-up, correct
      landing/bonus/fail semantics, and time-based trail/effect decay.
- [ ] Prove Chrome resize across wide, narrow, and tall desktop viewports preserves camera projection,
      readable gameplay/HUD, correct input coordinates, and bounded render targets/objects.
- [ ] Record every found gameplay/runtime defect with seed, fixed-tick actions or replay, root cause,
      and regression coverage.
- [ ] Run all Bounce Run tests, affected engine/pool/graph/replay tests, typecheck/build, and user-Chrome
      long-run/30 FPS/resize smoke with only the established Rapier warning.

## Required context

- `docs/autonomous-engine-game-agent.md`
- `docs/engine-game-development-plan.md` M13 only
- `docs/node-graph-architecture.md` scheduler, activation/pool, events, and diagnostics sections
- `docs/stage-handoff.md`
- `docs/handoffs/M13-05.md`
- `apps/bounce-run/src/main.ts`
- `apps/bounce-run/src/game-systems.ts`
- `apps/bounce-run/src/infinite-route.ts`
- `apps/bounce-run/src/replay.ts`
- `apps/bounce-run/src/qa-observations.ts`
- `apps/bounce-run/src/effects-composition.ts`
- Narrow public scheduler, pool metrics, physics presentation, renderer info, and QA observation
  entrypoints found through `rg`

## Decisions and invariants

- Audio is complete and out of scope. Do not modify audio composition, assets, graph nodes, buses,
  unlock/pause ordering, music ownership, or HUD audio controls.
- Bounded effects composition is complete. Preserve the public typed service/backend, capacities,
  accepted landing/bonus/fail authorities, one scheduler, and Three ownership boundaries unless a
  RED proves a defect in those contracts.
- Long-run evidence may add read-only diagnostics but never a QA mutation shortcut, autoplay bot,
  second RAF/clock, direct raw Scene access in app code, or production-export QA dependency.
- Measure a warm baseline before choosing heap/FPS tolerances. GC and `performance.memory` are noisy;
  use repeated retained plateaus and exact object/handle counters as the authoritative leak signal.
- Breaking fixes remain allowed, but every affected repository consumer must be repaired in the same
  stage without compatibility layers.

## Completed

- M13 generator, runtime variants/bonus, graph score/high score, production audio, and bounded
  presentation effects/trail slices are complete.
- Engine effects budgets are explicit: 18 burst slots, 12 queued requests, 28 trail samples, and 19
  Three scene objects (18 meshes plus one Points object), with shared GPU resources and exactly-once
  disposal.
- The bounded integration proves 100 accepted emissions and 20 reset/pool cycles without active,
  owned-handle, subscription, or render growth. This is focused lifecycle evidence, not the required
  long-run heap/FPS/resize acceptance.

## Files changed

- `packages/engine/src/presentation-effects.ts`: typed bounded backend/service and Three ownership.
- `packages/engine/src/presentation-effects.test.ts`: budget, pause, cleanup, validation, and disposal.
- `packages/engine/src/engine.ts`, `packages/engine/src/render-backend.ts`: typed runtime composition.
- `apps/bounce-run/src/effects-composition.ts`: gameplay visual language and pool lifecycle cleanup.
- `apps/bounce-run/src/effects-composition.integration.test.ts`: mandatory RED/GREEN and lifecycle load.
- `apps/bounce-run/src/game-systems.ts`, `apps/bounce-run/src/landing-tracker.ts`: accepted event and
  LateUpdate trail boundaries.
- `apps/bounce-run/src/infinite-route.ts`, `apps/bounce-run/src/main.ts`: variant styling and runtime
  composition.
- `docs/engine-game-development-plan.md`: bounded visual-loop evidence.

## Local commits

- `0039720` — `Specify bounded Bounce Run presentation effects`
- `dec0082` — `Add bounded engine presentation effects backend`
- `8a0a158` — `Harden presentation effect ownership cleanup`
- `47df80a` — `Compose Bounce Run effects and visual language`
- `78701a4` — `Record M13 bounded visual loop evidence`

## Verification evidence

- Mandatory RED: `pnpm exec vitest run apps/bounce-run/src/effects-composition.integration.test.ts`
  — failed before production changes because `./effects-composition.js` did not exist.
- Focused GREEN: engine effects, Bounce effects/game/landing/route, engine/pool lifecycle — 7 files /
  28 tests passed; only the established Rapier warning appeared.
- All Bounce Run: `pnpm exec vitest run apps/bounce-run/src` — 20 files / 76 tests passed.
- Affected engine/pool/graph/runtime: 33 files / 119 tests passed.
- `pnpm --filter @haku/engine build` — passed.
- `pnpm --filter @haku/bounce-run typecheck` — passed.
- `pnpm --filter @haku/bounce-run build` — passed, 202 modules; 3,230.64 kB minified /
  1,093.75 kB gzip JS; established chunk-size advisory only.
- User Chrome on agent-owned `http://127.0.0.1:5194/` — distinct normal/wide/narrow/bounce visual
  language and contrasting ball/trail visible; restart returned to `pool 6/8`, QA diagnostics stayed
  zero, and console contained only the established Rapier warning. Fail state occurred naturally,
  but fail/bonus bursts were not captured visually. Agent server stopped.

## Current repository state

- Branch: `engine-improvement-based-on-game-creation`
- HEAD before the separate handoff/evidence commits: `47df80a`
- Worktree contains only protected user changes plus documentation before commit: modified
  `apps/playground/haku.project.json` and untracked `apps/playground/public/assets/ui/`.
- Neither protected path was read, modified, staged, restored, deleted, reformatted, overwritten, or
  copied. User Vite PID 69639 remains the listener on port 5174.

## Remaining work

- Complete the final M13 stabilization stage below.
- Keep full gameplay-loop visual coherence and no-sustained-growth acceptance unchecked until that
  long-run and multi-viewport evidence is green.
- M14 final export/quality audit remains out of scope until M13 stabilization is complete.

## Risks and open defects

- Focused bounded counters do not establish retained heap stability over a long browser session.
- User Chrome visually proved platform language and trail, but not the brief fail burst or pickup
  spark; automated accepted-boundary tests currently own those claims.
- 30 FPS render cadence and wide/narrow/tall resize behavior have not yet been exercised in this M13
  stage.

## Exact next action

Before changing production runtime, add
`apps/bounce-run/src/stabilization.integration.test.ts`. Build the real production-like Bounce Run
world with Rapier, scheduler, deterministic seed, graph session, real platform and bonus pools, and
the public controllable presentation backend. Capture the authored baseline after warm-up, then run
at least 72,000 fixed ticks (20 simulated minutes), at least 10,000 route pool transitions, and at
least 100 fail/restart/release/reacquire cycles while replaying fixed-tick lateral actions. The first
RED must assert exact stable platform/bonus totals, active world entities, queued graph work,
effect/trail/owned-handle/subscription counts, and zero post-dispose render objects; also compare a
30 FPS frame schedule with the 60 FPS reference for identical fixed-tick state hashes and time-based
effect expiry. Run only this file and capture the first real growth, cadence, cleanup, or missing
read-only-metrics failure before implementing a fix. After headless GREEN, use user Chrome with a
reproducible seed/replay for a warm long-run sample and wide/narrow/tall viewport changes; record any
defect as seed + replay + regression before repair. Do not begin export or M14.

## Context exclusions

- Do not read or modify `apps/playground/haku.project.json` or
  `apps/playground/public/assets/ui/hud.ui.json`.
- Do not reopen Bounce Run audio composition, audio assets, audio graph nodes, or audio HUD controls.
- Do not redesign the effects API or change its budgets without a stabilization RED demonstrating
  the need.
- Do not begin M14, static export, storage redesign, visual editor, mobile input, or platform adapters.
