# Handoff: M13 final isolated audit

## Objective

Audit the completed M13 long-run stabilization implementation from a fresh context. Run the full
repository gates without changing runtime behavior, mark M13 complete only if they remain green,
then hand off the exact first M14 RED. Do not combine this audit with M14 implementation.

## Completed stabilization work

- Mandatory RED: `f806850` added a production-like real Rapier/scheduler/graph/pool/effects run for
  72,000 fixed ticks after 120 warm ticks, at least 10,000 pool acquisitions/releases, and 100
  deterministic fail/restart cycles using seed `0x13_06`.
- `4f1ac70` added the narrow public scheduler ownership snapshot required by the RED.
- The second RED measured 8,328 session trace entries against the 4,096 budget. A targeted
  five-entry-capacity graph RED retained 21 records before `e677b3a` implemented bounded circular
  trace retention with chronological reads and monotonic sequence IDs.
- `6e684b7` exposed immutable dev-only runtime observations for world entities, scheduler queue and
  registered systems, bonus pool, effects/render objects, and effect subscriptions.
- `5a574f9` exposed the same already-public bounded counters in the visible QA status for user-Chrome
  observation; it adds no mutation shortcut or production-export dependency.
- `docs/evidence/m13-stabilization-replay.v1.json` records the fixed-tick action schedule, exact
  transition/failure actions, both stabilization defects, and the earlier accepted-failure trail
  regression fixed at `842c036`.

## Verification evidence

- Stabilization GREEN: tick 72,120; at least 10,000 acquisitions and releases; 100 fail/restart
  cycles; exact warm platform/bonus/world/scheduler/effect/subscription restoration; bounded session
  trace; zero post-dispose effect subscriptions, handles, and render objects.
- Cadence GREEN: 60 FPS × 600 frames and 30 FPS × 300 frames produce identical 600 fixed-tick
  hashes; maximum one/two fixed steps; equal fail-effect lifetime; bounded irregular-frame overflow.
- Focused stabilization, scheduler, graph, pool, engine effects/lifecycle, and replay gate: 10 files /
  38 tests passed. The only stderr was the established Rapier initialization deprecation warning.
- Affected QA observation/harness/report gate: 3 files / 29 tests passed. Bounce Run typecheck passed.
- User Chrome extension only, agent-owned server `127.0.0.1:5196`: exact 1600×900, 900×1200, and
  720×1280 canvas backing/CSS and HUD bounds, no document overflow, visible HUD/actions, three warm
  visible restart cycles, stable 21 entities, bonus 1/1, queue zero, two subscriptions, zero QA
  diagnostics, and trail 28→0 on game-over. The viewport was reset, the agent tab finalized, and the
  server was stopped after the run.
- `performance.memory` was unavailable through Chrome extension isolation. Do not infer precise heap
  bytes, forced-GC behavior, or a device-tier threshold. Repeated warm plateaus and exact public
  ownership counters are the authoritative leak evidence.

## Official sources used

- MDN `performance.now()`: monotonic high-resolution elapsed time.
  <https://developer.mozilla.org/en-US/docs/Web/API/Performance/now>
- MDN `requestAnimationFrame()`: display-cadence callback and background pause behavior.
  <https://developer.mozilla.org/en-US/docs/Web/API/window/requestAnimationFrame>
- MDN `performance.memory`: deprecated, non-standard, unreliable heap estimates.
  <https://developer.mozilla.org/en-US/docs/Web/API/Performance/memory>
- Chrome DevTools memory guidance: retained/live plateaus, detached objects, and heap snapshot noise.
  <https://developer.chrome.com/docs/devtools/memory-problems>
  <https://developer.chrome.com/docs/devtools/memory-problems/heap-snapshots>
- Playwright viewport guidance used by the Chrome extension.
  <https://playwright.dev/docs/api/class-page#set-viewport-size>
  <https://playwright.dev/docs/api/class-browser#browser-new-context>

## Current repository state

- Branch: `engine-improvement-based-on-game-creation`.
- Stabilization implementation HEAD before these documentation commits: `5a574f9`.
- Protected user state remains modified `apps/playground/haku.project.json` plus untracked
  `apps/playground/public/assets/ui/`. Neither protected path was read, modified, staged, restored,
  deleted, reformatted, overwritten, or copied.
- User Vite PID 69639 remains the listener on port 5174. The stabilization run used only the
  agent-owned port 5196.
- Audio composition/assets/nodes/buses/HUD controls were not modified. No M14 or export work began.

## Exact next action

From a fresh context, inspect only the committed M13 stabilization diff and protected-aware status,
then run the isolated full repository gates (`pnpm test`, repository formatting/lint checks required
by package scripts, and the relevant builds/typechecks). Do not rerun Chrome unless a gate exposes a
browser-specific regression. If every gate is green, update the M13 acceptance rows and milestone
table to Complete in a documentation-only commit, then create a fresh handoff whose exact next action
is only the first mandatory M14 RED. If a gate fails, add the narrowest deterministic RED and fix
only that regression before reconsidering M13 completion.

## Context exclusions

- Do not read or modify `apps/playground/haku.project.json` or
  `apps/playground/public/assets/ui/hud.ui.json`.
- Do not reopen completed Bounce Run audio or redesign bounded effects budgets without a new RED.
- Do not begin M14 implementation, export, storage redesign, editor work, or platform adapters during
  the isolated M13 audit.
