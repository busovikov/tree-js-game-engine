# Handoff: M13 completion and first M14 RED

## Objective

Close M13 only after an isolated committed-HEAD audit maps every acceptance row to code, tests,
deterministic evidence, and the existing user-Chrome observations. Preserve the user's live
playground state and hand off one exact production-export RED without implementing M14.

## Scope and acceptance criteria

- [x] Map all eight M13 acceptance rows to committed code, tests, browser observations, and evidence.
- [x] Run the configured repository lint, typecheck, test, build, dependency, canonical-check, and
      production Bounce Run boundary gates in a detached clean worktree.
- [x] Validate the versioned stabilization replay and exact long-run/cadence counters.
- [x] Mark the M13 milestone and all eight acceptance rows Complete only after green configured gates.
- [x] Preserve honest Chrome extension, heap, audibility, and brief-effect capture limitations.
- [x] Remove the detached worktree only after a second explicit scene restore and tracked-clean check.
- [x] Identify, but do not implement, the first bounded M14 static-ZIP RED.

## Required context

- `docs/autonomous-engine-game-agent.md`
- `docs/engine-game-development-plan.md`: completed M13 and pending M14 acceptance
- `docs/node-graph-architecture.md`: production/export boundaries
- `docs/evidence/m13-stabilization-replay.v1.json`
- `packages/build/src/browser-static-zip.ts`
- `apps/bounce-run/index.html`
- `apps/bounce-run/vite.config.ts`
- `apps/bounce-run/package.json`
- The new M14 test named in **Exact next action**; no other M13 implementation needs reopening

## Decisions and invariants

- M13 is complete. No isolated gate exposed a source regression, so this stage changed documentation
  only and did not manufacture a code fix.
- Existing committed Chrome evidence remains authoritative because no runtime code changed and no
  browser-specific regression emerged. The audit did not rerun Chrome.
- Exact bounded ownership counters and repeated warm plateaus prove retained-state stability.
  Chrome-extension isolation did not expose `performance.memory`, so precise heap bytes, forced-GC
  behavior, and a universal device-tier threshold remain unclaimed.
- Audio browser evidence proves successful unlock and visible state transitions, not subjective
  audibility. The brief fail burst and bonus pickup were not captured visually and remain covered by
  focused accepted-boundary tests.
- The repository has no configured root formatting script. A non-mutating diagnostic Prettier check
  reported the existing 507-file baseline; it is not represented as a green configured gate and no
  repo-wide reformat was attempted.
- The next stage begins with a RED against the real Bounce Run production output and the generic
  `@haku/build` ZIP writer. It must not replace browser-local export with a server service or ship QA,
  editor, compiler-source, Monaco, React Flow adapter, or remote runtime dependencies.

## Completed

- Marked M13 Complete in the milestone map and checked all eight acceptance rows.
- Recorded exact generator mass, stabilization, replay, cadence, Chrome viewport, production
  boundary, and full-gate evidence beside M13.
- Confirmed the evidence artifact parses as version 1 / M13 / seed 4870, totals 72,120 ticks, requires
  at least 10,000 acquisitions and releases, records 100 fail/restart cycles, and names all three
  regressions.
- Ran the full gate matrix from detached committed HEAD `f4b285c` with the ignored 159-file manifest
  prerequisite and no main-worktree build writes.
- Narrowly inspected only the existing static export/ZIP and Bounce Run production entry boundaries
  needed to define the first M14 RED. No M14 source or test was written.

## Files changed

- `docs/engine-game-development-plan.md`: mark M13 complete and record the final acceptance/gate map.
- `docs/handoffs/M13-07.md`: final M13 operational record and exact first M14 RED.

## Local commits

- `f806850` — `Add M13 long-run stabilization RED`
- `4f1ac70` — `Expose bounded scheduler ownership metrics`
- `e677b3a` — `Bound graph execution trace retention`
- `6e684b7` — `Expose bounded Bounce Run runtime observations`
- `5a574f9` — `Expose stabilization counters in QA status`
- `3273836` — `Record M13 stabilization replay evidence`
- `f4b285c` — `Hand off M13 final isolated audit`
- `0b52340` — `Mark M13 complete after isolated repository gates`
- This handoff is committed separately after verification.

## Verification evidence

- Temp prerequisite: `node scripts/generate-isaac-sketch-scenes.mjs` generated the ignored
  159-file `apps/playground/public/assets/manifest.json`; all six generator-touched tracked Isaac
  scenes were explicitly restored before and after gates.
- `pnpm install --frozen-lockfile` — passed for all 22 workspace projects.
- `pnpm lint` — passed with no ESLint findings.
- `pnpm typecheck` — passed all 21 runnable projects after the clean-worktree package repacks.
- Focused stabilization command over scheduler, graph instance/lifecycle, pool, engine effects and
  lifecycle, Bounce replay, and the long-run integration — 10 files / 37 tests passed. The real long
  run reached 72,120 ticks; only the established Rapier initialization deprecation warning appeared.
- Evidence validation — parsed exact version/stage/seed/tick/transition/cycle/regression values;
  SHA-256 `87da25be86694005d2e200438bd606f68409063e1e38817b0be0e403f1ea7e5a`.
- `pnpm test` — 205 files / 824 tests passed; 1 file / 8 tests skipped. This included the real Bounce
  Run production QA-boundary build and all eight DEV-QA sentinel exclusions.
- `pnpm build` — passed all 21 runnable projects. Bounce Run built 202 modules at 3,231.32 kB minified
  / 1,093.90 kB gzip; only the established chunk-size advisory appeared.
- `pnpm depcruise` — no new violations across 604 modules / 1,185 dependencies; five known baseline
  violations ignored.
- `./scripts/check.sh` — repeated install, all-project build, the 205-file/824-test suite, and
  playground bundle audit; final output `OK: all checks passed`.
- Fresh `file:` package copies required three successive
  `pnpm install --force --frozen-lockfile` repacks as schema/core, serializer/Rapier/engine, and editor
  declaration layers became available. Intermediate missing-module failures were packaging order,
  not source regressions; final build and typecheck passed.
- Formatting: there is no configured root format script. Diagnostic
  `pnpm exec prettier --check .` reported the established 507-file baseline without writing files;
  the two changed documentation files pass their own Prettier checks before commit.
- Browser check: unchanged committed user-Chrome evidence covers the complete loop, responsive
  1600×900 / 900×1200 / 720×1280 layouts, stable warm counters, and zero QA diagnostics. No new
  Chrome session was run because the audit changed no runtime behavior.
- Final temp check: the six tracked scenes were restored again, tracked and staged diffs were empty,
  the ignored manifest remained confined to the temp worktree, and the worktree was removed.

## Current repository state

- Branch: `engine-improvement-based-on-game-creation`
- HEAD before this separate handoff commit: `0b52340`
- Main worktree contains this handoff plus exactly the protected user changes:
  - modified `apps/playground/haku.project.json`;
  - untracked `apps/playground/public/assets/ui/hud.ui.json` (Git may summarize its directory).
- Neither protected file was read, modified, staged, restored, deleted, reformatted, overwritten, or
  copied. Their sizes and mtimes remained 3,238/671 bytes and `1785346231` throughout the audit.
- User Vite PID 69639 remains the listener on port 5174 and was not stopped, replaced, or contacted.
- The detached gate worktree `/tmp/haku-m13-gates.Qc4dpt` was removed after its tracked-clean check.
- No push, PR, history rewrite, M14 implementation, or additional server/browser session occurred.

## Remaining work

- Execute M14 sequentially, beginning only with the RED below.
- After its GREEN, prove the extracted archive in the user's Chrome under a nested static-server base,
  record same-origin-only requests and console state, and scan authoritative production exclusions.
- Complete the remaining M14 quality, measured-budget, documentation, deferred-backlog, and final
  handoff rows in later bounded stages.

## Risks and open defects

- Narrow inspection found a concrete M14 export risk: the current Bounce Run Vite production HTML
  emits a root-absolute `src="/assets/index-*.js"`. Such a ZIP requests the server root rather than
  its extracted nested directory, so nested-base portability is not yet proven for Bounce Run.
- The browser-static-export Worker currently recognizes only its curated runtime-module surface;
  do not claim that feeding raw Bounce Run TypeScript through it works until a RED exercises the
  intended production entry path.
- The repository-wide 507-file Prettier baseline and three-repack clean-install ordering remain
  quality/tooling debt for M14 to document or fix in separately scoped work, not hidden M13 failures.

## Exact next action

Create `apps/bounce-run/src/production-static-zip.integration.test.ts` as the mandatory first M14
RED. Add `@haku/build: workspace:*` only as a Bounce Run dev dependency so the test imports
`createBrowserStaticExportZip` from `@haku/build`. The test must run the real production build,
archive only its emitted `dist` files, extract the ZIP into a clean nested path such as
`deployments/preview/v1/`, and first assert that every emitted HTML script/style/asset URL is
relative to that extracted root. Run exactly:

```text
pnpm exec vitest run apps/bounce-run/src/production-static-zip.integration.test.ts
```

Expected first failure: the relative-URL assertion reports the current generated
`src="/assets/index-<hash>.js"`; under the nested server it would escape to `/assets/...`. Commit that
RED before changing `apps/bounce-run/vite.config.ts` or export composition. The minimal GREEN must
then preserve the generic `@haku/build` ZIP path, prove `unzip -t`, root `index.html`, no source or
unreachable files, portable file modes, and authoritative bundle exclusions. Finish the same bounded
slice with a clean extracted nested static server in user Chrome, recording only same-origin archive
requests (no CDN/external requests), gameplay readiness, console diagnostics, and final cleanup. Do
not broaden this RED into the rest of M14 documentation or performance work.

## Context exclusions

- Do not read or modify `apps/playground/haku.project.json` or
  `apps/playground/public/assets/ui/hud.ui.json`.
- Do not reopen M13 audio, effects, generator, score/save, stabilization, QA observation, or replay
  implementations without a new deterministic regression.
- Do not load editor authoring subsystems for the first RED; the generic ZIP writer and real Bounce
  Run production output are sufficient.
- Do not begin the remaining M14 documentation/performance sweep before the static-ZIP slice is green
  and handed off.
