# Handoff: M14-03 release-surface documentation and create scaffold

## Objective

Audit the executable M14 release surface against actual workspace manifests, public exports,
and emitted create-template files; correct stale package/API/example/template/user documentation
without running the final repository gates or declaring M14 complete.

## Scope and acceptance criteria

- [x] Commit the mandatory release-surface audit RED before docs/template corrections.
- [x] Derive workspace package names, export specifiers, and scaffold output files in the test.
- [x] Map graph/compiler/runtime/scheduler/checkpoint/prefab/pool/physics/UI/audio/storage/
      platform/build/QA/replay/generator/Bounce Run/static ZIP capabilities to real owners.
- [x] Correct the create template and local `file:` engine dependency closure.
- [x] Correct existing tech stack, public API, package graph, examples, user docs, limitations,
      and deferred-backlog surfaces in place.
- [x] Keep M14 in progress and defer the isolated full final audit/browser/ZIP completion gate.

## Required context

- `docs/engine-game-development-plan.md`: M14 acceptance and deferred backlog
- `docs/handoffs/M14-02.md`: bundle/browser evidence and protected-state constraints
- `packages/create/src/m14-release-surface-documentation.integration.test.ts`
- `docs/architecture.md`, `docs/links.md`, `docs/techstack.md`, `README.md`

## Decisions and invariants

- Package names, dependencies, public subpaths, and template output are derived from manifests,
  `exports`, and files rather than prose or invented APIs.
- Shipped games compose the runtime packages they use; they never depend on the editor or React.
- Local create-scaffold linking recursively follows production `@haku/*` dependencies from the
  selected engine manifest. It no longer maintains a stale handwritten sibling list.
- Vite entry and base are relative. The scaffold uses public `@haku/engine/runtime` and
  `@haku/assets` imports and contains no editor/QA/CDN/server coupling.
- Browser-authoring locations are `public/assets/graphs/*.graph.json`,
  `public/assets/types/*.type.json`, `public/assets/components/*.component.json`,
  `src/gameplay.ts`, `src/editor-extension.ts`, and `scripts/*.ts`; manifest UUID is identity.
- The user-Chrome 30 Hz evidence is an alert, not a 60 Hz pass/fail. No exact heap-byte claim is
  made. Mobile/touch and real platform adapters remain deferred.
- The required `read-safety-language-interaction-rules` and linked `read-*` helpers were absent;
  available project rules and in-place-only documentation constraints were used as fallback.

## Completed

- Added a seven-test executable inventory and capability/deferred/documentation audit.
- Corrected the generated scaffold's relative production surface and typecheck command.
- Replaced the fake future player hook with a pure typed project-algorithm example.
- Fixed local engine linking after a real generated install exposed missing audio/graph/runtime/
  pool/UI workspace dependencies and a stale Rapier override.
- Added the complete public entrypoint inventory, current dependency graph, 18-ID capability
  matrix, engine-only examples, authoring layout, deployment workflow, and honest limitations.
- Marked only the two M14 documentation/deferred acceptance rows complete; M14 remains in progress.

## Files changed

- `packages/create/src/m14-release-surface-documentation.integration.test.ts`: executable audit.
- `packages/create/src/index.ts`: manifest-derived local production dependency closure.
- `packages/create/templates/`: relative Vite entry/base, typecheck, real algorithm example.
- `packages/create/README.md`: scaffold commands and relative deployment contract.
- `README.md`, `docs/README.md`: minimal user entrypoints and engine-only examples.
- `docs/architecture.md`: actual graph, capability matrix, authoring layout, limitations.
- `docs/techstack.md`, `docs/links.md`: exact package roles and public export specifiers.
- `docs/edge-cases.md`: current release limitations instead of the obsolete ScriptRef stub claim.
- `docs/engine-game-development-plan.md`: documentation/deferred rows only.

## Local commits

- `dfc89b2` — `Add M14 release surface audit RED`
- `a6ac96a` — `Make create template production-ready`
- `ef55196` — `Document the M14 release surface`
- This handoff is committed separately.

## Verification evidence

- Mandatory RED:
  `pnpm exec vitest run packages/create/src/m14-release-surface-documentation.integration.test.ts`
  failed 1 file / 6 tests on package/export docs, capability matrix, deferred wording, examples,
  scaffold relativity, and authoring/workflow/limitations documentation.
- Local-link RED: the focused manifest-closure test failed because the handwritten overrides
  omitted audio, graph, graph-runtime, pool, and UI and incorrectly included physics-rapier.
- Final focused audit: the same Vitest command passed 1 file / 7 tests.
- `pnpm --filter @haku/create typecheck` — passed.
- `pnpm --filter @haku/create build` — passed.
- Fresh generated local-link project: `pnpm install`, `pnpm typecheck`, and `pnpm build` passed;
  Vite built 167 modules and 817.24 kB minified JS with only its standard 500 kB advisory.
- `git diff --check` — passed before both GREEN commits.
- Browser check — not run; this stage changed no runtime gameplay and found no template runtime
  regression after the real generated production build.

## Current repository state

- Branch: `engine-improvement-based-on-game-creation`
- HEAD before this separate handoff commit: `ef55196`
- Worktree contains only this handoff plus protected user changes:
  - modified `apps/playground/haku.project.json`;
  - untracked `apps/playground/public/assets/ui/hud.ui.json` (Git summarizes its directory).
- Neither protected file was read, modified, staged, restored, deleted, reformatted, overwritten,
  or copied. User Vite PID 69639 / port 5174 was not contacted, stopped, or replaced.
- Generated-project temporary directories were moved to Trash after verification.

## Remaining work

- Run a fresh isolated full repository format/lint/typecheck/test/build audit.
- Recreate the production ZIP from a clean directory, run it through a simple static server, and
  complete the final user-Chrome session using the established production exclusions.
- If and only if every final gate is green, mark M14 and the full MVP complete and write the final
  handoff listing all local commits and residual risks.

## Risks and open defects

- A broad Prettier check was intentionally not applied: focused checking reported existing style
  warnings in several large Markdown/HTML files, and M14-03 forbids repo-wide reformat. The final
  audit must distinguish configured format gates from optional wholesale Markdown normalization.
- The generated minimal template builds one 817.24 kB chunk and receives Vite's generic 500 kB
  advisory; M14-03 did not add performance/bundle work or a template-specific budget.
- M14-02's 30 Hz extension-surface limitation and lack of precise heap telemetry remain.
- No full repository gate or final browser/clean-ZIP session was run here.

## Exact next action

Start a fresh isolated M14 final-gates stage. Run the repository's configured full
format/lint/typecheck/test/build audit first, correct only reproducible release regressions, then
create a clean production ZIP and exercise it from a simple static HTTP server in the user Chrome.
Only after all gates are green, mark M14 and the full MVP Complete and create the final handoff.

## Context exclusions

- Do not read or modify `apps/playground/haku.project.json` or
  `apps/playground/public/assets/ui/hud.ui.json`.
- Do not reopen M13 gameplay/audio/generator/replay internals without a failing final gate.
- Do not change the proven M14-02 Rapier chunk split or budgets without a deterministic regression.
- Do not rerun the M14-03 documentation audit unless one of its covered files changes.
