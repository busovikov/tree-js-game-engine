# Handoff: UI-M0 baseline characterization and test harness

## Objective

Lock the intended version 1 UI document/runtime/editor baseline, preserve the two known authoring
defects as explicit expected failures, and establish stable real-editor Chrome evidence before the
breaking M1 schema/runtime work begins.

## Scope and acceptance criteria

- [x] Characterize strict trees and current layout translation without duplicating existing cases.
- [x] Characterize selection, full-asset command undo, save validation, and storage failure paths.
- [x] Keep lossy `100%`/`auto` Inspector sizing and fixed `0.5` top-left preview as green-suite
      expected failures with desired future assertions.
- [x] Provide a strict four-element built-in editor fixture and stable `data-haku-ui-*` selectors.
- [x] Capture default, larger preview-preset, and narrow viewport evidence in the user's Chrome.
- [x] Keep runtime/editor test harness code out of the production game bundle.
- [x] Pass focused package tests and typechecks.
- [x] Preserve the protected playground work and commit only explicit M0 paths.

## Required context

- `docs/ui-editor-figma-development-plan.md` — Status, Baseline, M0, and Definition of Done.
- `docs/ui-kit.md` — Runtime UI authoring and required component patterns.
- `docs/edge-cases.md` — Runtime UI documents.
- `packages/ui/src/schema.ts`
- `packages/ui/src/ui-document-instance.ts`
- `packages/ui/src/ui-document-instance.test.ts`
- `packages/ui/src/ui-runtime.behavior.test.ts`
- `packages/editor/src/ui/ui-authoring-session.ts`
- `packages/editor/src/ui/ui-authoring-session.test.ts`
- `packages/editor/src/ui/UIDocumentEditorPanel.tsx`
- `packages/editor/src/ui/ui-document-editor-panel.css`
- `packages/editor/src/ui/ui-editor-service.ts`
- `packages/editor/src/ViewportTabsShell.test.tsx`

## Decisions and invariants

- M0 does not fix the Inspector sizing or preview navigation defects; M5 and M3 respectively own
  removal of their `it.fails` markers.
- The browser fixture is editor-only, strict, and uses the existing built-in UI document path.
- Stable evidence selectors are `data-haku-ui-workspace`, `data-haku-ui-document-path`,
  `data-haku-ui-selected-id`, `data-haku-ui-tree-item`, `data-haku-ui-selected`,
  `data-haku-ui-preview`, `data-haku-ui-preview-scale`, `data-haku-ui-preview-origin`,
  `data-haku-ui-action`, and `data-haku-ui-status`.
- Protected paths remain outside all reads, edits, staging, and commits.

## Completed

- Added named missing-child, multiply-parented, and cyclic strict-tree cases alongside the existing
  duplicate/unreachable coverage.
- Added current flex/wrap/alignment/gap, pixel/percentage/auto sizing, flex sizing, and anchor DOM
  translation coverage.
- Added unknown selection, strict full-asset replacement/undo, invalid-save atomicity, and rejected
  storage-save coverage.
- Added four-element editor fixture/path/selection/preview selector coverage.
- Captured explicit defect RED, restored expected-failure markers, and finished with green suites.
- Captured Chrome default, larger preview-preset, and narrow viewport screenshots.

## Files changed

- `packages/ui/src/ui-document-instance.test.ts`: current layout translation characterization.
- `packages/ui/src/ui-runtime.behavior.test.ts`: missing/multiple-parent/cycle strict-tree cases.
- `packages/editor/src/ui/ui-authoring-session.test.ts`: selection, full-asset undo, validation, and
  save rejection cases.
- `packages/editor/src/ViewportTabsShell.test.tsx`: selector/fixture proof and expected failures.
- `packages/editor/src/ui/UIDocumentEditorPanel.tsx`: stable editor-only evidence selectors.
- `packages/editor/src/ui/ui-editor-service.ts`: strict four-element built-in fixture.
- `docs/handoffs/evidence/UI-M0-01-default.png`: default Chrome baseline.
- `docs/handoffs/evidence/UI-M0-01-zoomed.png`: 1920 × 1080 preview-preset baseline.
- `docs/handoffs/evidence/UI-M0-01-narrow.png`: 900 × 700 editor viewport baseline.
- `docs/ui-editor-figma-development-plan.md`: completed M0 status and evidence.
- `docs/handoffs/UI-M0-01.md`: this stage record.

## Local commits

- `5c9522a` — `test: lock the UI editor M0 baseline`
- The documentation-only commit containing this handoff records the M0 status and evidence links.

## Verification evidence

- Initial focused baseline runs — `@haku/ui` 10 tests and `@haku/editor` 143 tests passed before
  changes.
- RED browser contract — the new regular workspace case failed because the path/selectors were not
  present; implementation then made it green.
- Defect RED: `pnpm exec vitest run packages/editor/src/ViewportTabsShell.test.tsx -t
  'round-trips|fits and centers'` — three intentional failures: received `0` vs `100%`, `0` vs
  `auto`, and `0.5` vs `fit`.
- `pnpm --filter @haku/ui test -- src/ui-document-instance.test.ts
  src/ui-runtime.behavior.test.ts` — pass, 3 files / 14 tests.
- `pnpm --filter @haku/editor test -- src/ui/ui-authoring-session.test.ts
  src/ViewportTabsShell.test.tsx` — pass, 47 files / 151 tests, including expected failures.
- `pnpm --filter @haku/ui typecheck` — pass.
- `pnpm --filter @haku/editor typecheck` — pass.
- Chrome default at 1920 × 864 — built-in path, four tree elements, root selected, preview preset
  `desktop-1280x720`, computed transform `matrix(0.5, 0, 0, 0.5, 0, 0)`, origin `0px 0px`.
- Chrome selection — `Hint` selected in hierarchy and production preview; stable selected ID matches;
  Inspector reports the characterized lossy width `0` for serialized `auto`.
- Chrome larger preset — serialized 1920 × 1080 preview renders 960 × 540 under the same fixed
  transform and origin.
- Chrome narrow at 900 × 700 — workspace remains visible, path/selection/four elements remain
  exposed, no document-level horizontal overflow, and console error count is zero.
- Built-in Save is disabled, matching the documented read-only behavior.

## Current repository state

- Branch: `engine-improvement-based-on-game-creation`
- Implementation commit: `5c9522a`
- Worktree after this documentation commit should contain only the pre-existing protected user work:
  `M apps/playground/haku.project.json` and `?? apps/playground/public/assets/ui/`.
- Protected hashes supplied by the user were not recomputed because those files were intentionally
  not read; no M0 command staged or committed either path.

## Remaining work

- M0 has no remaining implementation work.
- M1 is the next sequential stage: version 2 model, required elements, and React-free runtime.

## Risks and open defects

- Inspector width/height still coerce serialized percentage and `auto` values to `0`; M5 owns the
  fix and expected-failure marker removal.
- Preview remains fixed at scale `0.5` with top-left origin; M3 owns centered fit/navigation and
  expected-failure marker removal.
- The narrow baseline records existing panel clipping inside the editor shell; M0 only
  characterizes it and does not redesign the workspace.

## Exact next action

Start a fresh M1 stage, read only the M1 section of the plan plus the listed schema/runtime
entrypoints, and begin RED-first tests for the sole version 2 serialized contract and required
React-free runtime elements. Do not modify the protected playground paths.

## Context exclusions

- `apps/playground/haku.project.json`
- `apps/playground/public/assets/ui/**`
- Scene, graph, audio, storage/platform, and unrelated editor subsystems.
- M2 and later implementation work beyond preserving the M3/M5 expected-failure ownership notes.
