import { RAPIER_PHYSICS_CAPABILITIES } from '@haku/physics-rapier'

/** Editor play mode uses Rapier — gate inspector fields against its capabilities. */
export const EDITOR_PHYSICS_CAPABILITIES = RAPIER_PHYSICS_CAPABILITIES
